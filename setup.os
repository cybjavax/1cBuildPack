#Использовать cmdline

Парсер = Новый ПарсерАргументовКоманднойСтроки();
Парсер.ДобавитьПараметр("repo");

Параметры = Парсер.Разобрать(АргументыКоманднойСтроки);

Если ЗначениеЗаполнено(Параметры["repo"]) Тогда
	Репо = Параметры["repo"];
Иначе
	Репо = "";
КонецЕсли;

ЗапуститьПриложение("oscript setupgitclone.os "+Репо, ,Истина);
ЗапуститьПриложение("oscript setupjsonbehavior.os", ,);
ЗапуститьПриложение("oscript setupjsonbehavior.os \testclone", ,);
